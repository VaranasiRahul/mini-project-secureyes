apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: apps
spec:
  selector:
    app: api
  ports:
    # Named port — required so the ServiceMonitor can reference it by name
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
