apiVersion: v1
kind: Namespace
metadata:
  name: apps
  labels:
    # Enforce restricted Pod Security Standard on all pods in this namespace.
    # This blocks root containers, privileged mode, and hostPath volumes.
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/warn: restricted
