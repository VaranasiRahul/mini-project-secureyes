apiVersion: v2
name: loki-stack-wrapper
description: Wrapper pinning loki-stack (Loki + Promtail) version
version: 0.1.0
dependencies:
  - name: loki-stack
    version: "2.10.2"
    repository: "https://grafana.github.io/helm-charts"
